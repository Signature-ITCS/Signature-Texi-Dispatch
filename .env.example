# ─────────────────────────────────────────────────────────────────────────────
# TaxiFlow — environment
#
# Copy to `.env.local` for development, or paste into the hosting dashboard.
# Every value below is a placeholder; nothing real belongs in this file, which
# IS committed. Real keys go in `.env.local`, which is not.
#
# Standing up a fresh instance? Build the database first — see db/README.md.
# ─────────────────────────────────────────────────────────────────────────────

# ── Supabase ─────────────────────────────────────────────────────────────────
# URL and anon key: Project Settings → API. The anon key is public by design;
# row-level security is what protects the data, not the key.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key: Project Settings → API Keys → service_role → Reveal.
# Bypasses row-level security completely. Server-side only — never expose it,
# and never give it a NEXT_PUBLIC_ prefix.
SUPABASE_SERVICE_ROLE_KEY=

# The `api_key` of the row in `websites` this deployment serves.
# `insert into websites (name, slug) values ('Main Website', 'main');` then read
# back the generated key.
WIDGET_API_KEY=

# ── The name customers see ──────────────────────────────────────
# Shown on the tracking page, the booking widget, the install page and the
# browser tab. Falls back to "TaxiFlow" -- the software's name, not the firm's,
# which means nothing to a passenger.
NEXT_PUBLIC_BRAND_NAME=Your Taxi Company

# ── Site ─────────────────────────────────────────────────────────────────────
# Public base URL, no trailing slash. Used to build Stripe return URLs and the
# links in customer emails, so it must match the real domain in production.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── Google Maps ──────────────────────────────────────────────────────────────
# Two keys, because they can only be locked down in opposite ways.
#
# Browser key — inlined into the page, so anyone can read it. Its one real
# defence is an HTTP referrer restriction listing your domains. Enable only:
# Maps JavaScript API.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Server key — never leaves the server, so there is no referrer to check.
# Restrict it by API instead (Places API, Directions API) and set a daily quota
# cap. If left blank the browser key is used, which works but leaves you unable
# to restrict either one properly.
GOOGLE_MAPS_API_KEY=

# ── Payments (optional) ──────────────────────────────────────────────────────
# Card payments stay OFF unless this is exactly "on". With it off the booking
# form offers cash to driver only, and the checkout API refuses to start a
# session even if called directly.
NEXT_PUBLIC_CARD_PAYMENTS=off

# Stripe. Use sk_test_… until you have tested the whole flow end to end.
STRIPE_SECRET_KEY=

# From the webhook endpoint you create at Stripe → Developers → Webhooks,
# pointing at https://your-domain/api/webhooks/stripe. Without it the webhook
# rejects every event, and a customer who closes the tab mid-payment loses the
# booking they just paid for.
STRIPE_WEBHOOK_SECRET=

# ── Email (optional) ─────────────────────────────────────────────────────────
# Booking confirmations and staff invites. Without a key, email is skipped and
# the booking still succeeds.
RESEND_API_KEY=
# Must be an address at a domain verified in Resend.
EMAIL_FROM=bookings@your-domain.co.uk

# ── SMS (optional) ───────────────────────────────────────────────────────────
COSMICSMS_USERNAME=
COSMICSMS_API_PASSWORD=
COSMICSMS_SENDER=
